// Skill_Schema_Modular_v2.txt
// Final accepted changes: maxLevel, xpCurve, skillCategory, description 
{
  "skillId": "string",                // required, unique
  "displayName": "string",
  
  "skillCategory": "combat-melee|combat-ranged|gathering|crafting",   // UPDATED

  "skillCategory": "combat|gathering|crafting",
  "description": "string",

  // v1 cap — allows future expansion without breaking save schema
  "maxLevel": 20,

  // XP curve is data-driven, not hardcoded (Phase 1.4 requirement) :contentReference[oaicite:6]{index=6}
  "xpCurve": [
    {
      "level": 1,
      "xpToNext": 50
    }
  ],

  // Optional per-level effects (unlocks, passive bonuses, etc.)
  "effects": [
    {
      "level": 1,
      "effectType": "unlock_recipe|stat_bonus|other",
      "value": "recipeId_or_other_payload"
    }
  ]
}
